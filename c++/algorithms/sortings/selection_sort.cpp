#include<bits/stdc++.h>
 
using namespace std;

void selectionSort(int arr[], int n) {
    int min = INT_MAX;
    for(int i = 0; i < n; i++) {
        if(arr[i] < min) {
            
        }
   }
}

int main() {
    int arr[] = { 64, 25, 12, 22, 11 };
    n = sizeof(arr) / sizeof(arr[0]);
    selectionSort(arr, n);
    return 0;
}
